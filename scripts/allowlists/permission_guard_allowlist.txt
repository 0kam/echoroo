# Allowlist for scripts/lint_permission_guard.py.
#
# Phase 2.11 P1-a: line-level fingerprint format. Each non-comment
# line is <file>:<function_name>:missing-permission-guard. The
# fingerprint is locked to a SPECIFIC violation so adding a new
# unguarded endpoint to a file that already has allowlisted entries
# fails the lint (the previous file-level allowlist silently inherited
# every new violation in the same file).
#
# Phase 3 must drive each entry to empty before final cleanup.
# Adding a new entry requires a justifying PR comment.
#
# Inline comments use TWO SPACES + #. Lines beginning with #
# are full-line comments. See scripts/allowlists/README.md.

# Phase 2.11 P1-a baseline established 2026-04-25 (replacing the file-
# level baseline from Phase 2.10 #7). Each entry inherits the same
# "Phase 3 US11 T1xx" cleanup target.
# W2-3 PR-11: the 14 ``api/v1/admin.py`` routes (users / settings / licenses /
# recorders) were unmounted (now helpers delegated to by the ``/web-api/v1/admin/*``
# BFF, which gate_action-guards each handler via ``_admin_users`` / ``_admin_settings``
# / ``_admin_licenses`` / ``_admin_recorders`` — those BFF entries are retained below).
# The legacy handlers keep ``_gate_admin_platform_action`` internally but no longer
# register a route, so their own allowlist entries were dropped.
# W2-3 PR-6: the eight browser-facing ``api/v1/annotation_sets.py`` CRUD/palette/
# nested-segments routes were unmounted (now helpers delegated to by the
# project-scoped ``/web-api/v1/projects/{project_id}/annotation-sets/*`` BFF,
# which gate_action-guards each handler — no thin-adapter allowlist entry
# required). ``dispatch_sampling`` (POST ``/{set_id}/sample``) stays mounted and
# unguarded, so its entry is retained below.
apps/api/echoroo/api/v1/annotation_sets.py:dispatch_sampling:missing-permission-guard  # legacy: Phase 3 US11 T1xx
# W2-4 PR-A: the three ``api/v1/clips.py`` media routes (audio / spectrogram /
# download) were unmounted (now helpers). ``download_clip`` is delegated to by
# the ``/web-api/v1`` BFF (``_media.py:download_clip``, which gate_action-guards
# itself). ``get_clip_audio`` / ``get_clip_spectrogram`` have no BFF twin. The
# routes are gone, so all three dead fingerprints were dropped here.
apps/api/echoroo/api/v1/confirmed_regions.py:create_confirmed_region:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/confirmed_regions.py:delete_confirmed_region:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/confirmed_regions.py:list_confirmed_regions:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/datasets.py:export_dataset:missing-permission-guard  # legacy: Phase 3 US11 T1xx
# W2-3 PR-17: the 10 browser-superseded ``api/v1/detections.py`` routes
# (list/species-summary/export-csv/export-ml-dataset/temporal-data/create/
# change-species and the three vote handlers) were unmounted from ``/api/v1``
# in favour of the project-scoped ``/web-api/v1`` BFF (``_detections.py`` /
# ``_detection_export.py`` / ``_votes.py`` delegates import these bodies as
# helpers). The dead fingerprints (change_species/create_detection/export_csv/
# export_ml_dataset/get_species_summary/get_temporal_data/list_detections)
# were dropped; the vote handlers never had entries (centrally gated). The 4
# single-detection routes (get/confirm/reject/delete) stay mounted, so their
# entries are retained below.
apps/api/echoroo/api/v1/detections.py:confirm_detection:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/detections.py:delete_detection:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/detections.py:get_detection:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/detections.py:reject_detection:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/h3.py:get_h3_from_coordinates:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/h3.py:validate_h3:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/projects.py:add_project_member:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/projects.py:create_project:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/projects.py:delete_project:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/projects.py:get_project:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/projects.py:get_project_overview:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/projects.py:list_project_members:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/projects.py:list_projects:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/projects.py:remove_project_member:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/projects.py:update_project:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/projects.py:update_project_member_role:missing-permission-guard  # legacy: Phase 3 US11 T1xx
# W2-3 PR-1: ``api/v1/recorders.py:list_recorders`` route unmounted (now a
# helper delegated to by the ``/web-api/v1/recorders`` BFF). The surviving BFF
# entry for ``web_v1/_recorders.py:list_recorders`` is retained below.
# W2-3 PR-16: the 6 browser-superseded recording routes (list/get/update/
# delete/playback/spectrogram) were unmounted from ``/api/v1``; their dead
# fingerprints (get_recording/update_recording/delete_recording/
# get_playback_audio/get_spectrogram; list_recordings had no entry) were
# dropped. The audio / stream media routes stay mounted and unguarded, so their
# entries are retained below.
# W2-4 PR-A: the ``download`` route was unmounted (now a helper delegated to by
# the ``/web-api/v1`` BFF ``_media.py:download_recording``, which gate_action-
# guards itself), so its dead ``download_recording`` fingerprint was dropped.
apps/api/echoroo/api/v1/recordings.py:stream_audio:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/recordings.py:stream_audio_legacy:missing-permission-guard  # legacy: Phase 3 US11 T1xx
# W2-3 PR-18: the 10 browser-superseded ``api/v1/search/sessions.py`` routes
# (list / get / delete / update / rerun / export-recordings / export-csv /
# distribution / time-distribution / sample) and
# ``api/v1/search/similarity.py:get_embedding_stats`` were unmounted from
# ``/api/v1`` in favour of the project-scoped ``/web-api/v1/.../search*`` BFF
# (``_search.py`` delegates import these bodies as helpers). Their dead
# fingerprints were dropped; the ``batch.py`` handlers never had entries
# (they gate_action-guard themselves). The two ``similar`` / ``similar-by-audio``
# POSTs stay mounted, so their entries are retained below.
# W2-4 PR-B: ``stream_reference_audio`` was unmounted (now a helper delegated to
# by the ``/web-api/v1`` BFF ``_search.py:stream_reference_audio``, which
# gate_action-guards itself), so its dead fingerprint was dropped.
apps/api/echoroo/api/v1/search/similarity.py:search_similar:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/search/similarity.py:search_similar_by_audio:missing-permission-guard  # legacy: Phase 3 US11 T1xx
# W2-3 PR-3: ``api/v1/segments.py`` routes unmounted (now helpers delegated to by
# the project-scoped ``/web-api/v1/projects/{project_id}/segments/*`` BFF, which
# gate_action-guards each handler — no thin-adapter allowlist entry required).
apps/api/echoroo/api/v1/settings.py:get_embedding_model:missing-permission-guard  # legacy: Phase 3 US11 T1xx
# W2-3 PR-8: the five ``api/v1/sites.py`` routes were unmounted (now helpers
# delegated to by the ``/web-api/v1/.../sites/*`` BFF, which gate_action-guards
# each handler — no thin-adapter allowlist entry required).
# W2-3 PR-9: the seven ``api/v1/tags.py`` routes were unmounted (now helpers
# delegated to by the ``/web-api/v1/.../tags/*`` BFF). The mutating BFF handlers
# (create/update/delete) gate_action-guard themselves, so no entry is needed for
# them; the four BFF read endpoints stay thin adapters (their allowlist entries
# are retained below).
apps/api/echoroo/api/v1/taxa.py:create_taxon_from_gbif:missing-permission-guard  # WS-A: ungated global taxa reference write (auth-only, no project scope), like search_taxa/gbif_search_taxa
apps/api/echoroo/api/v1/taxa.py:gbif_search_taxa:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/taxa.py:get_taxon:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/taxa.py:list_taxa:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/taxa.py:search_taxa:missing-permission-guard  # legacy: Phase 3 US11 T1xx
# W2-3 PR-4: ``api/v1/time_range_annotations.py`` routes unmounted (now helpers
# delegated to by the project-scoped ``/web-api/v1/projects/{project_id}/
# annotations/*`` BFF, which gate_action-guards each handler — no thin-adapter
# allowlist entry required).
# W2-3 PR-10: the three ``api/v1/uploads.py`` routes were unmounted (now helpers
# delegated to by the ``/web-api/v1/.../upload-sessions*`` BFF; the two mutating
# BFF handlers gate_action-guard themselves and the read endpoint stays a thin
# adapter — its ``_uploads.py`` entry is retained below).
apps/api/echoroo/api/v1/users.py:change_password:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/users.py:create_api_token:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/users.py:get_current_user:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/users.py:list_api_tokens:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/users.py:revoke_api_token:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/users.py:update_current_user:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/v1/xeno_canto.py:proxy_sonogram:missing-permission-guard  # legacy: Phase 3 US11 T1xx
apps/api/echoroo/api/web_v1/auth.py:verify_email:missing-permission-guard  # public pre-session auth route; project/user permission guard not applicable

# W2-2 B+C self-scoped BFF mirrors of /api/v1/users/me* — each delegates verbatim
# to the legacy api/v1/users.py handler (allowlisted above) and operates strictly
# on the authenticated session user (no project context). Classified
# USER_SCOPED_ONLY in core/endpoint_allowlist.py; like the legacy entries they
# carry no Permission guard. (The line-level lint is independent of the structured
# allowlist, so each function needs an explicit fingerprint here.)
apps/api/echoroo/api/web_v1/users.py:get_current_user_me:missing-permission-guard  # W2-2 B+C: BFF mirror of /api/v1/users/me — auth-only, no project scope
apps/api/echoroo/api/web_v1/users.py:update_current_user:missing-permission-guard  # W2-2 B+C: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/users.py:change_password:missing-permission-guard  # W2-2 B+C: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/users.py:list_api_tokens:missing-permission-guard  # W2-2 B+C: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/users.py:create_api_token:missing-permission-guard  # W2-2 B+C: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/users.py:revoke_api_token:missing-permission-guard  # W2-2 B+C: thin adapter (legacy unguarded)

# spec/009 PR 3a thin BFF adapters (Phase 3 US11 T1xx scope). The BFF delegates
# to the legacy handler and adds no guard of its own pending TAG_LIST/GET/GBIF/
# STATISTICS/UPLOAD_GET Action introduction. Note: the legacy ``api/v1/tags.py``
# (W2-3 PR-9) and ``api/v1/uploads.py`` (W2-3 PR-10) handlers are now plain
# importable helpers — their routes were unmounted, so their own allowlist
# entries were dropped, and only these BFF thin-adapter entries remain.
apps/api/echoroo/api/web_v1/projects/_tags.py:gbif_suggest:missing-permission-guard  # spec/009 PR 3a: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/projects/_tags.py:get_statistics:missing-permission-guard  # spec/009 PR 3a: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/projects/_tags.py:get_tag:missing-permission-guard  # spec/009 PR 3a: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/projects/_tags.py:list_tags:missing-permission-guard  # spec/009 PR 3a: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/projects/_uploads.py:get_upload_session_status:missing-permission-guard  # spec/009 PR 3a: thin adapter (legacy unguarded)

# spec/009 PR 4 thin BFF adapter (Phase 3 US11 T1xx scope). The BFF delegates
# to ``echoroo.api.v1.recorders.list_recorders``, which since W2-3 PR-1 is a
# plain importable helper (its ``/api/v1/recorders`` route was unmounted, so it
# no longer needs its own allowlist entry). This ``/web-api/v1/recorders`` route
# adds no guard of its own — the public recorder catalog has no project_id to
# gate on; auth alone is sufficient.
apps/api/echoroo/api/web_v1/_recorders.py:list_recorders:missing-permission-guard  # spec/009 PR 4: thin adapter (legacy unguarded)

# spec/009 PR 5 thin admin BFF adapters — sibling of the 14
# api/v1/admin.py entries above. Each handler calls the module-local
# ``_gate_admin_platform_action`` helper (mirrors the legacy v1
# pattern), which in turn calls ``is_allowed`` directly. The lint
# walks the path operation function body only, so the inner is_allowed
# is invisible and the legacy parity allowlist applies here too.
# Phase 3 US11 T1xx cleanup will replace the helper invocation with
# a canonical ``gate_action`` once the platform-scope Action surface
# grows a project-less binding.
apps/api/echoroo/api/web_v1/_admin_licenses.py:create_license:missing-permission-guard  # spec/009 PR 5: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/_admin_licenses.py:delete_license:missing-permission-guard  # spec/009 PR 5: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/_admin_licenses.py:get_license:missing-permission-guard  # spec/009 PR 5: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/_admin_licenses.py:list_licenses:missing-permission-guard  # spec/009 PR 5: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/_admin_licenses.py:update_license:missing-permission-guard  # spec/009 PR 5: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/_admin_recorders.py:create_recorder:missing-permission-guard  # spec/009 PR 5: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/_admin_recorders.py:delete_recorder:missing-permission-guard  # spec/009 PR 5: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/_admin_recorders.py:get_recorder:missing-permission-guard  # spec/009 PR 5: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/_admin_recorders.py:list_recorders:missing-permission-guard  # spec/009 PR 5: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/_admin_recorders.py:update_recorder:missing-permission-guard  # spec/009 PR 5: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/_admin_settings.py:get_system_settings:missing-permission-guard  # spec/009 PR 5: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/_admin_settings.py:update_system_settings:missing-permission-guard  # spec/009 PR 5: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/_admin_users.py:list_users:missing-permission-guard  # spec/009 PR 5: thin adapter (legacy unguarded)
apps/api/echoroo/api/web_v1/_admin_users.py:update_user:missing-permission-guard  # spec/009 PR 5: thin adapter (legacy unguarded)

# spec/011 US7 (T600-T602) in-app banner + activity endpoints. These operate
# strictly on the authenticated session user (resolved by CurrentUser); there
# is no project context and no cross-user side effect, so they carry NO
# gate_action project-permission guard — the same USER_SCOPED_ONLY trust
# boundary (OQ1) as /web-api/v1/users/me, change-password, and the step-up
# endpoints. They are classified USER_SCOPED_ONLY in core/endpoint_allowlist.py;
# the Action model has no user-self scope to bind. Cookie + CSRF transport is
# enforced by the production middleware chain.
apps/api/echoroo/api/web_v1/me.py:dismiss_me_banner:missing-permission-guard  # spec/011 US7: USER_SCOPED_ONLY (no project scope)
apps/api/echoroo/api/web_v1/me.py:list_me_activity:missing-permission-guard  # spec/011 US7: USER_SCOPED_ONLY (no project scope)
apps/api/echoroo/api/web_v1/me.py:list_me_banners:missing-permission-guard  # spec/011 US7: USER_SCOPED_ONLY (no project scope)
